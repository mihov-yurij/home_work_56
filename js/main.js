import { asyncOperationDemo } from './index.js';

// Запускаємо демонстрацію
// Передаємо порожній колбек, оскільки логування вже вбудоване у функцію
asyncOperationDemo((op) => {
  // Тут можна обробити результат, якщо потрібно
});
